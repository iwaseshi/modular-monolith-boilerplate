{
    "version": "0.2.0",
    "inputs": [
        {
            "type": "pickString",
            "id": "debugTarget",
            "description": "Please select the launch api ",
            "options": [
                "main.g",
            ]
        }
    ],
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/main.go",
            "env": {},
        }
    ]
}
