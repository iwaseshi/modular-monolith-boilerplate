{
    "version": "0.2.0",
    "inputs": [
        {
            "type": "pickString",
            "id": "debugTarget",
            "description": "Please select the launch api ",
            "options": [
                "main.go",
            ]
        }
    ],
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/${input:debugTarget}",
            "env": {},
            "args": []
        }
    ]
}
